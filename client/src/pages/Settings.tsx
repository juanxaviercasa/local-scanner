import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { PageHeader } from "@/components/ScannerUI";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, KeyRound, Save, ShieldCheck, SlidersHorizontal } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { toast } from "sonner";

export default function Settings() { const { user } = useAuth(); const budget = trpc.settings.budget.useQuery(); const profiles = trpc.settings.scoringProfiles.useQuery(); const integrations = trpc.integrations.status.useQuery(); const [values, setValues] = useState({ dailyRequestBudget: 100, monthlyRequestBudget: 2000, maxCostPerRunCents: 0, maxBusinessesPerRun: 20, maxAiCallsPerRun: 0 }); const [weights, setWeights] = useState(""); useEffect(() => { if (budget.data) setValues({ dailyRequestBudget: budget.data.dailyRequestBudget, monthlyRequestBudget: budget.data.monthlyRequestBudget, maxCostPerRunCents: budget.data.maxCostPerRunCents, maxBusinessesPerRun: budget.data.maxBusinessesPerRun, maxAiCallsPerRun: budget.data.maxAiCallsPerRun }); }, [budget.data]); useEffect(() => { if (profiles.data?.[0]) setWeights(JSON.stringify(profiles.data[0].weights, null, 2)); }, [profiles.data]); const utils = trpc.useUtils(); const updateBudget = trpc.settings.updateBudget.useMutation({ onSuccess: () => { utils.settings.budget.invalidate(); toast.success("Límites actualizados."); }, onError: error => toast.error(error.message) }); const updateScore = trpc.settings.updateScoring.useMutation({ onSuccess: () => { utils.settings.scoringProfiles.invalidate(); toast.success("Ponderaciones actualizadas."); }, onError: error => toast.error(error.message) }); const updateProfile = trpc.profile.update.useMutation({ onSuccess: () => toast.success("Perfil actualizado."), onError: error => toast.error(error.message) }); const saveBudget = (event: FormEvent) => { event.preventDefault(); updateBudget.mutate(values); }; const saveWeights = () => { const profile = profiles.data?.[0]; if (!profile) return; try { const parsed = JSON.parse(weights); if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) throw new Error(); updateScore.mutate({ profileId: profile.id, weights: parsed as Record<string, number> }); } catch { toast.error("Usa un objeto JSON con claves y valores numéricos."); } }; return <DashboardLayout><PageHeader eyebrow="Controles del sistema" title="Configuración y fuentes" description="Ajusta el consumo, mantén visible la configuración de cada proveedor y adapta la puntuación a tu estrategia sin editar el código." /><div className="grid gap-6 xl:grid-cols-[1fr_1fr]"><form onSubmit={saveBudget} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-100 text-violet-700"><ShieldCheck size={18} /></span><div><h2 className="font-bold">Límites de consumo</h2><p className="mt-1 text-sm text-slate-500">La previsualización bloqueará cualquier plan que los supere.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{[["Límite diario de solicitudes","dailyRequestBudget"],["Límite mensual de solicitudes","monthlyRequestBudget"],["Coste máximo por ejecución (céntimos)","maxCostPerRunCents"],["Máximo de negocios por ejecución","maxBusinessesPerRun"],["Máximo de llamadas IA por ejecución","maxAiCallsPerRun"]].map(([label,key]) => <label key={key} className="text-sm font-semibold">{label}<Input className="mt-2" type="number" min="0" value={values[key as keyof typeof values]} onChange={event => setValues({ ...values, [key]: Number(event.target.value) })} /></label>)}</div><Button className="mt-6" type="submit" disabled={updateBudget.isPending}><Save size={16} />Guardar límites</Button></form><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><KeyRound size={18} /></span><div><h2 className="font-bold">Estado de proveedores</h2><p className="mt-1 text-sm text-slate-500">Los valores permanecen en variables de entorno y nunca se exponen al cliente.</p></div></div><div className="mt-6 space-y-3">{Object.entries(integrations.data || {}).map(([name, config]) => <div className="flex items-center justify-between rounded-2xl bg-slate-50 px-4 py-3" key={name}><span><strong className="block text-sm">{name === "googleMaps" ? "Google Places mediante proxy autorizado" : name === "googleSheets" ? "Google Sheets" : "Analizador de sitio web"}</strong><small className="text-xs text-slate-500">{config.mode === "official_proxy" ? "Fuente oficial con autenticación gestionada" : "Placeholder configurable"}</small></span><span className={config.configured ? "flex items-center gap-1 text-xs font-bold text-emerald-700" : "text-xs font-bold text-amber-700"}>{config.configured ? <><CheckCircle2 size={14} />Disponible</> : "Pendiente"}</span></div>)}</div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-amber-100 text-amber-700"><SlidersHorizontal size={18} /></span><div><h2 className="font-bold">Puntuación explicable</h2><p className="mt-1 text-sm text-slate-500">Los pesos se almacenan por perfil y determinan cómo se interpreta una señal verificada.</p></div></div><textarea className="mt-5 min-h-60 w-full rounded-xl border border-input bg-slate-950 p-4 font-mono text-xs leading-6 text-emerald-200 outline-none focus:ring-2 focus:ring-primary" value={weights} onChange={event => setWeights(event.target.value)} aria-label="Pesos de puntuación" /><Button className="mt-4" disabled={updateScore.isPending || !profiles.data?.length} onClick={saveWeights}><Save size={16} />Guardar pesos</Button></section><form onSubmit={event => { event.preventDefault(); const fd = new FormData(event.currentTarget); updateProfile.mutate({ name: String(fd.get("name") || "").trim() || null, timezone: String(fd.get("timezone") || "UTC") }); }} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold">Perfil del operador</h2><p className="mt-1 text-sm text-slate-500">Preferencias básicas de la cuenta actual.</p><label className="mt-5 block text-sm font-semibold">Nombre<Input name="name" className="mt-2" defaultValue={user?.name || ""} /></label><label className="mt-4 block text-sm font-semibold">Zona horaria<Input name="timezone" className="mt-2" defaultValue={Intl.DateTimeFormat().resolvedOptions().timeZone || "UTC"} /></label><Button type="submit" className="mt-5" disabled={updateProfile.isPending}>Guardar perfil</Button></form></div></DashboardLayout>; }
