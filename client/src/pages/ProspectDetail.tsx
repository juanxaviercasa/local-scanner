import DashboardLayout from "@/components/DashboardLayout";
import { PageHeader, PriorityPill } from "@/components/ScannerUI";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CheckCircle2, ExternalLink, Globe2, MapPin, Phone, Save, Star } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";
import { Link, useRoute } from "wouter";

const labels: Record<string, string> = { websiteOpportunityScore: "Oportunidad web", businessAttractivenessScore: "Atractivo de negocio", digitalOpportunityScore: "Oportunidad digital", commercialPotentialScore: "Potencial comercial", leadPotentialScore: "Potencial de lead", urgencyScore: "Urgencia" };
export default function ProspectDetail() { const [, params] = useRoute("/app/prospectos/:id"); const id = Number(params?.id); const item = trpc.prospects.get.useQuery({ prospectId: id }, { enabled: Boolean(id) }); const [notes, setNotes] = useState(""); useEffect(() => { if (item.data) setNotes(item.data.prospect.notes || ""); }, [item.data]); const utils = trpc.useUtils(); const update = trpc.prospects.update.useMutation({ onSuccess: () => { utils.prospects.get.invalidate({ prospectId: id }); toast.success("Seguimiento actualizado."); }, onError: error => toast.error(error.message) }); if (item.isLoading) return <DashboardLayout><div className="h-72 animate-pulse rounded-3xl bg-slate-200" /></DashboardLayout>; if (!item.data) return <DashboardLayout><p className="text-sm text-slate-500">No se encontró esta oportunidad.</p></DashboardLayout>; const { prospect, business, run } = item.data; const scores = ["businessAttractivenessScore", "digitalOpportunityScore", "websiteOpportunityScore", "commercialPotentialScore", "leadPotentialScore", "urgencyScore"] as const; return <DashboardLayout><Link href="/app/prospectos" className="mb-5 inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-primary"><ArrowLeft size={16} />Volver a oportunidades</Link><PageHeader eyebrow={`Ejecución ${run.publicId}`} title={business.name} description={`${business.category || "Categoría no disponible"} · ${business.address || [business.city, business.country].filter(Boolean).join(", ") || "Ubicación no disponible"}`} actions={<PriorityPill priority={prospect.priority} />} /><div className="grid gap-6 xl:grid-cols-[1.45fr_.85fr]"><div className="space-y-6"><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-primary">Opportunity Score</p><strong className="mt-2 block text-6xl tracking-[-0.06em]">{prospect.opportunityScore}<small className="text-xl text-slate-400">/100</small></strong></div><div className="max-w-md rounded-2xl bg-violet-50 p-4 text-sm leading-6 text-violet-950">{prospect.analysisSummary}</div></div><div className="mt-7 grid gap-3 sm:grid-cols-2">{prospect.scoreReasons?.length ? prospect.scoreReasons.map((reason: { label: string; points: number }, index: number) => <div key={`${reason.label}-${index}`} className="flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3"><span className="text-sm text-slate-600">{reason.label}</span><strong className={reason.points > 0 ? "text-emerald-700" : "text-rose-700"}>{reason.points > 0 ? "+" : ""}{reason.points}</strong></div>) : <p className="text-sm text-slate-500">No hay razones de puntuación disponibles.</p>}</div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold">Datos disponibles</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-4"><MapPin size={17} className="text-primary" /><strong className="mt-3 block text-sm">Ubicación</strong><p className="mt-1 text-sm leading-6 text-slate-500">{business.address || "No encontrada"}</p></div><div className="rounded-2xl bg-slate-50 p-4"><Phone size={17} className="text-primary" /><strong className="mt-3 block text-sm">Teléfono</strong><p className="mt-1 text-sm leading-6 text-slate-500">{business.phone || "No encontrado"}</p></div><div className="rounded-2xl bg-slate-50 p-4"><Globe2 size={17} className="text-primary" /><strong className="mt-3 block text-sm">Sitio web</strong><p className="mt-1 break-all text-sm leading-6 text-slate-500">{business.website || "No encontrado"}</p>{business.website && <a href={business.website} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-xs font-bold text-primary">Abrir sitio <ExternalLink size={13} /></a>}</div><div className="rounded-2xl bg-slate-50 p-4"><Star size={17} className="text-primary" /><strong className="mt-3 block text-sm">Ficha pública</strong><p className="mt-1 text-sm leading-6 text-slate-500">{business.rating ?? "No disponible"} · {business.reviewCount ?? 0} reseñas</p>{business.googleMapsUrl && <a href={business.googleMapsUrl} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-xs font-bold text-primary">Abrir fuente <ExternalLink size={13} /></a>}</div></div></section></div><aside className="space-y-6"><section className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-[0.14em] text-violet-300">Subpuntuaciones</p><div className="mt-5 space-y-4">{scores.map(score => <div key={score}><div className="flex items-center justify-between text-sm"><span className="text-slate-300">{labels[score]}</span><strong>{prospect[score]}</strong></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-violet-400" style={{ width: `${prospect[score]}%` }} /></div></div>)}</div><div className="mt-7 border-t border-white/10 pt-5"><p className="text-xs text-slate-400">Tipos de oportunidad</p><div className="mt-2 flex flex-wrap gap-2">{prospect.opportunityTypes?.length ? prospect.opportunityTypes.map((type: string) => <span key={type} className="rounded-full bg-white/10 px-2.5 py-1 text-[10px] font-bold tracking-wide">{type}</span>) : <span className="text-sm text-slate-400">Sin clasificación</span>}</div></div></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold">Seguimiento interno</h2><p className="mt-2 text-sm leading-6 text-slate-500">Estas notas no modifican los datos de la fuente ni de aplicaciones posteriores.</p><Textarea className="mt-4 min-h-32" value={notes} onChange={event => setNotes(event.target.value)} placeholder="Añade contexto para tu próxima revisión…" /><div className="mt-4 flex gap-2"><select className="h-10 min-w-0 flex-1 rounded-md border border-input bg-background px-2 text-sm" defaultValue={prospect.status} onChange={event => update.mutate({ prospectId: id, status: event.target.value as "new" | "qualified" | "rejected" | "exported" | "analysis_pending" | "analyzed" | "demo_pending" | "contact_pending" | "contacted" | "converted" | "lost" })}><option value="new">Nuevo</option><option value="qualified">Aprobado</option><option value="rejected">Descartado</option><option value="exported">Exportado</option><option value="analysis_pending">Pendiente de auditoría</option></select><Button disabled={update.isPending} onClick={() => update.mutate({ prospectId: id, notes })}><Save size={16} />Guardar</Button></div></section></aside></div></DashboardLayout>; }
